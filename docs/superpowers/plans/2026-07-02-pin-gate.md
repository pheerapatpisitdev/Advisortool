# PIN Gate Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** เพิ่มด่านรหัสผ่าน 6 หลัก (soft gate ฝั่งเบราว์เซอร์) หน้าเข้าใช้งาน Advisortool ครอบคลุมทั้ง Hub และเครื่องมือย่อยทั้ง 10 ตัว

**Architecture:** โมดูลกลางที่ inject ตัวเอง (แบบเดียวกับ `assets/global-header.js`) รันใน `<head>` ก่อนเนื้อหาหน้าจะแสดง ถ้ายังไม่ปลดจะซ่อนหน้า+แสดง keypad เต็มจอ ตรวจ PIN แล้วเก็บสถานะ "ปลดแล้ว" ใน `localStorage` พร้อม idle timeout 12 ชม.

**Tech Stack:** Vanilla HTML/CSS/JS (ไม่มี build/framework/test runner) — verification ใช้ `node --check` (syntax) + checklist ในเบราว์เซอร์ (behavior) ตาม convention ของโปรเจกต์

**Spec:** `docs/superpowers/specs/2026-07-02-pin-gate-design.md`

---

## ⚠️ ก่อนเริ่ม (Preconditions)

ไฟล์ `index.html` ของเครื่องย่อยหลายตัว **มีการแก้ค้างอยู่ใน working tree** (ดู `git status`).
Task 4 จะแทรกโค้ดเข้าไฟล์เหล่านั้น เวลา `git add <file>` จะรวมการแก้ค้างเดิมไปด้วย

**ก่อนรันแผนนี้:** commit หรือ stash งานที่ค้างอยู่ก่อน เพื่อให้ commit ของ Task 4 มีเฉพาะการเพิ่ม pin-gate:
```bash
git status              # ดูว่ามีอะไรค้าง
git stash push -u -m "wip before pin-gate"   # หรือ commit งานที่ค้างตามปกติ
```

## File Structure

| ไฟล์ | สร้าง/แก้ | หน้าที่ |
|---|---|---|
| `assets/pin-gate.config.js` | สร้าง | ตั้งค่าเดียว: PIN + options (แก้รหัสที่นี่) |
| `assets/pin-gate.css` | สร้าง | สไตล์ฉากล็อก + keypad + ปุ่มล็อก (ธีม Navy & Gold, self-contained) |
| `assets/pin-gate.js` | สร้าง | ตรรกะ gate: เช็คสถานะ, keypad UI, ตรวจ PIN, lockout, ปุ่มล็อก |
| `index.html` (ราก) | แก้ | เพิ่ม 3 บรรทัด include ใน `<head>` (prefix `assets/`) |
| `{lifeready,ihealthy,ci123,ishield,fhc,career-agent,agency,global-saving,pension-smart95,group-insurance}/index.html` | แก้ | เพิ่ม 3 บรรทัด include ใน `<head>` (prefix `../assets/`) |

โหลด `pin-gate.config.js` **ก่อน** `pin-gate.js` เสมอ (gate อ่านค่าจาก `window.PIN_GATE_CONFIG`)

---

### Task 1: ไฟล์ config

**Files:**
- Create: `assets/pin-gate.config.js`

- [ ] **Step 1: สร้างไฟล์ config**

สร้าง `assets/pin-gate.config.js`:

```js
/* Advisortool — PIN gate configuration. แก้รหัสและตัวเลือกที่นี่ที่เดียว.
   หมายเหตุ: นี่คือ soft gate (ฝั่งเบราว์เซอร์) — ซอร์สเปิดดูได้ PIN นี้อ่านได้
   จากหน้าเว็บ ใช้กันคนทั่วไป ไม่ได้กันคนที่เปิดดูโค้ด. */
window.PIN_GATE_CONFIG = {
  pin: '123456',                        // รหัส 6 หลักของทีม — เปลี่ยนตรงนี้
  storageKey: 'az_gate',                // key ใน localStorage
  idleTimeoutMs: 12 * 60 * 60 * 1000,   // ไม่ใช้งานเกิน 12 ชม. → ล็อกใหม่ (sliding)
  maxAttempts: 5,                       // ใส่ผิดกี่ครั้งถึงล็อกชั่วคราว
  lockoutSeconds: 30,                   // ล็อกนานเท่าไร (วินาที)
  title: 'กรุณาใส่รหัสผ่าน',
  subtitle: 'Advisortool'
};
```

- [ ] **Step 2: ตรวจ syntax**

Run: `node --check assets/pin-gate.config.js`
Expected: ไม่มี output (exit 0 = syntax ถูกต้อง)

- [ ] **Step 3: Commit**

```bash
git add assets/pin-gate.config.js
git commit -m "feat(pin-gate): add gate config (PIN + options)"
```

---

### Task 2: ไฟล์ CSS

**Files:**
- Create: `assets/pin-gate.css`

- [ ] **Step 1: สร้างไฟล์ CSS**

สร้าง `assets/pin-gate.css`:

```css
/* Advisortool — PIN gate styles (ธีม Navy & Gold).
   Self-contained: ใช้ตัวแปรจาก theme.css ถ้ามี ไม่มีก็ fallback เป็นค่าจริง. */

/* ระหว่างล็อก: ซ่อนหน้าจริง + กัน scroll. overlay เป็น sibling ของ body จึงไม่โดนซ่อน. */
html.az-locked { overflow: hidden; background: var(--az-navy, #0d2c54); }
html.az-locked body { display: none !important; }

.az-pin {
  position: fixed; inset: 0; z-index: 2147483000;
  display: flex; align-items: center; justify-content: center; padding: 20px;
  background: linear-gradient(120deg, var(--az-navy, #0d2c54), var(--az-navy-2, #143b6e));
  font-family: var(--az-font, 'IBM Plex Sans Thai', 'Sarabun', 'Segoe UI', Tahoma, sans-serif);
  -webkit-tap-highlight-color: transparent;
  animation: az-pin-in .2s ease;
}
.az-pin--out { animation: az-pin-out .26s ease forwards; }
@keyframes az-pin-in { from { opacity: 0; } to { opacity: 1; } }
@keyframes az-pin-out { from { opacity: 1; } to { opacity: 0; } }

.az-pin__card {
  width: 100%; max-width: 340px; background: var(--az-card, #fff);
  border-radius: 18px; box-shadow: 0 20px 60px rgba(0,0,0,.35);
  padding: 28px 24px 24px; text-align: center;
}
.az-pin__brand {
  display: inline-block; font-size: 12px; font-weight: 700; letter-spacing: .5px;
  text-transform: uppercase; color: var(--az-gold-d, #a97f28);
  background: var(--az-gold-l, #f7efdd); padding: 4px 12px; border-radius: 20px;
}
.az-pin__title { margin-top: 14px; font-size: 18px; font-weight: 700; color: var(--az-navy, #0d2c54); }

.az-pin__dots { display: flex; justify-content: center; gap: 12px; margin: 20px 0 6px; }
.az-pin__dots span {
  width: 13px; height: 13px; border-radius: 50%;
  border: 2px solid var(--az-line, #e2e8f0); background: transparent;
  transition: transform .1s, background .1s, border-color .1s;
}
.az-pin__dots span.is-filled {
  background: var(--az-navy, #0d2c54); border-color: var(--az-navy, #0d2c54); transform: scale(1.05);
}

.az-pin__msg {
  min-height: 18px; font-size: 12.5px; color: var(--az-bad, #c0392b);
  font-weight: 500; opacity: 0; transition: opacity .15s;
}
.az-pin__msg--show { opacity: 1; }

.az-pin__pad { display: grid; grid-template-columns: repeat(3, 1fr); gap: 12px; margin-top: 14px; }
.az-pin__pad.is-disabled { opacity: .45; pointer-events: none; }
.az-pin__key {
  height: 62px; border: 0; border-radius: 14px; cursor: pointer;
  background: var(--az-soft, #eef3f8); color: var(--az-navy, #0d2c54);
  font-family: inherit; font-size: 24px; font-weight: 600;
  transition: transform .06s, background .12s; -webkit-user-select: none; user-select: none;
}
.az-pin__key:hover { background: var(--az-gold-l, #f7efdd); }
.az-pin__key:active { transform: scale(.94); background: var(--az-gold-l, #f7efdd); }
.az-pin__key--back { font-size: 20px; color: var(--az-muted, #64748b); }
.az-pin__spacer { visibility: hidden; }

.az-shake { animation: az-shake .38s cubic-bezier(.36,.07,.19,.97); }
@keyframes az-shake {
  10%, 90% { transform: translateX(-1px); }
  20%, 80% { transform: translateX(2px); }
  30%, 50%, 70% { transform: translateX(-5px); }
  40%, 60% { transform: translateX(5px); }
}

/* ปุ่มล็อกลอย (แสดงเมื่อปลดแล้ว) */
.az-lock-btn {
  position: fixed; left: 14px; bottom: 14px; z-index: 2147482000;
  width: 40px; height: 40px; border-radius: 50%;
  border: 1px solid var(--az-line, #e2e8f0);
  background: rgba(255,255,255,.9); color: var(--az-navy, #0d2c54);
  display: flex; align-items: center; justify-content: center; cursor: pointer;
  box-shadow: 0 2px 8px rgba(16,42,76,.18); opacity: .6;
  transition: opacity .15s, transform .1s;
  -webkit-backdrop-filter: blur(4px); backdrop-filter: blur(4px);
}
.az-lock-btn:hover { opacity: 1; }
.az-lock-btn:active { transform: scale(.92); }

@media print { .az-lock-btn { display: none !important; } }
@media (max-width: 380px) { .az-pin__key { height: 54px; font-size: 21px; } }
```

- [ ] **Step 2: ตรวจว่าไฟล์ไม่ว่างและมี selector หลักครบ**

Run: `grep -c "az-pin__key\|az-lock-btn\|html.az-locked" assets/pin-gate.css`
Expected: ตัวเลข ≥ 3 (มี class หลักครบ)

- [ ] **Step 3: Commit**

```bash
git add assets/pin-gate.css
git commit -m "feat(pin-gate): add lock screen + keypad styles (Navy & Gold)"
```

---

### Task 3: ไฟล์ตรรกะ gate

**Files:**
- Create: `assets/pin-gate.js`

- [ ] **Step 1: สร้างไฟล์ JS**

สร้าง `assets/pin-gate.js`:

```js
/* Advisortool — PIN gate (soft gate ฝั่งเบราว์เซอร์).
   ต้องใส่ PIN 6 หลักก่อนหน้าจะแสดง. include ใน <head> ของทุกหน้า หลัง
   pin-gate.config.js. รันแบบ synchronous เพื่อไม่ให้หน้าจริงแว้บก่อนฉากล็อก.
   หมายเหตุ: soft gate เท่านั้น — PIN อ่านได้ในซอร์ส. */
(function () {
  var CFG = window.PIN_GATE_CONFIG || {};
  var PIN = String(CFG.pin == null ? '' : CFG.pin);
  var KEY = CFG.storageKey || 'az_gate';
  var IDLE = CFG.idleTimeoutMs || 12 * 60 * 60 * 1000;
  var MAX = CFG.maxAttempts || 5;
  var LOCKOUT = (CFG.lockoutSeconds || 30) * 1000;
  var TITLE = CFG.title || 'กรุณาใส่รหัสผ่าน';
  var SUBTITLE = CFG.subtitle || 'Advisortool';
  var CONFIG_OK = /^\d{6}$/.test(PIN);

  var LOCK_ICON = '<svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>';

  var mem = null; // fallback ถ้า localStorage ใช้ไม่ได้ (โหมดส่วนตัว)

  function readState() {
    try { var raw = localStorage.getItem(KEY); return raw ? JSON.parse(raw) : null; }
    catch (e) { return mem; }
  }
  function writeState(obj) {
    mem = obj;
    try { localStorage.setItem(KEY, JSON.stringify(obj)); } catch (e) {}
  }
  function clearState() {
    mem = null;
    try { localStorage.removeItem(KEY); } catch (e) {}
  }
  function unlock() { writeState({ unlocked: true, ts: Date.now() }); }

  function isUnlocked() {
    if (!CONFIG_OK) return false;
    var s = readState();
    if (!s || s.unlocked !== true || typeof s.ts !== 'number') return false;
    if (Date.now() - s.ts > IDLE) return false;
    return true;
  }

  function esc(s) {
    return String(s).replace(/[&<>"]/g, function (c) {
      return { '&': '&amp;', '<': '&lt;', '>': '&gt;', '"': '&quot;' }[c];
    });
  }
  function onReady(fn) {
    if (document.body) fn();
    else document.addEventListener('DOMContentLoaded', fn);
  }
  function mountLockButton() {
    if (document.getElementById('az-lock-btn')) return;
    var b = document.createElement('button');
    b.id = 'az-lock-btn';
    b.type = 'button';
    b.className = 'az-lock-btn';
    b.title = 'ล็อก';
    b.setAttribute('aria-label', 'ล็อก');
    b.innerHTML = LOCK_ICON;
    b.addEventListener('click', function () { window.azLock(); });
    document.body.appendChild(b);
  }

  // public: ล็อกทันที (ใช้โดยปุ่มล็อกลอย)
  window.azLock = function () { clearState(); location.reload(); };

  // ปลดอยู่แล้ว → ต่ออายุ sliding timeout, เพิ่มปุ่มล็อก, แสดงหน้าปกติ
  if (isUnlocked()) {
    unlock();
    onReady(mountLockButton);
    return;
  }

  // ยังไม่ปลด → ซ่อนหน้า + แสดง keypad
  var root = document.documentElement;
  root.classList.add('az-locked');

  var overlay = document.createElement('div');
  overlay.className = 'az-pin';
  overlay.setAttribute('role', 'dialog');
  overlay.setAttribute('aria-modal', 'true');
  overlay.setAttribute('aria-label', TITLE);

  if (!CONFIG_OK) {
    overlay.innerHTML =
      '<div class="az-pin__card">' +
        '<div class="az-pin__title">ตั้งค่า PIN ไม่ถูกต้อง</div>' +
        '<div class="az-pin__msg az-pin__msg--show">กรุณาตั้ง pin เป็นตัวเลข 6 หลักใน assets/pin-gate.config.js</div>' +
      '</div>';
    root.appendChild(overlay);
    return;
  }

  function padBtn(d) { return '<button type="button" class="az-pin__key" data-d="' + d + '">' + d + '</button>'; }

  overlay.innerHTML =
    '<div class="az-pin__card">' +
      '<div class="az-pin__brand">' + esc(SUBTITLE) + '</div>' +
      '<div class="az-pin__title">' + esc(TITLE) + '</div>' +
      '<div class="az-pin__dots" aria-hidden="true"><span></span><span></span><span></span><span></span><span></span><span></span></div>' +
      '<div class="az-pin__msg" role="alert"></div>' +
      '<div class="az-pin__pad">' +
        padBtn('1') + padBtn('2') + padBtn('3') +
        padBtn('4') + padBtn('5') + padBtn('6') +
        padBtn('7') + padBtn('8') + padBtn('9') +
        '<span class="az-pin__spacer"></span>' +
        padBtn('0') +
        '<button type="button" class="az-pin__key az-pin__key--back" data-back aria-label="ลบ">⌫</button>' +
      '</div>' +
    '</div>';
  root.appendChild(overlay);

  var dots = overlay.querySelectorAll('.az-pin__dots span');
  var msgEl = overlay.querySelector('.az-pin__msg');
  var padEl = overlay.querySelector('.az-pin__pad');
  var cardEl = overlay.querySelector('.az-pin__card');

  var entered = '';
  var attempts = 0;
  var lockedUntil = 0;

  function renderDots() {
    for (var i = 0; i < dots.length; i++) dots[i].className = i < entered.length ? 'is-filled' : '';
  }
  function setMsg(text) {
    msgEl.textContent = text || '';
    msgEl.className = 'az-pin__msg' + (text ? ' az-pin__msg--show' : '');
  }
  function shake() {
    cardEl.classList.remove('az-shake');
    void cardEl.offsetWidth; // บังคับ reflow เพื่อเริ่ม animation ใหม่
    cardEl.classList.add('az-shake');
  }
  function press(d) {
    if (Date.now() < lockedUntil || entered.length >= 6) return;
    entered += d;
    renderDots();
    if (entered.length === 6) submit();
  }
  function back() {
    if (Date.now() < lockedUntil) return;
    entered = entered.slice(0, -1);
    renderDots();
  }
  function submit() {
    if (entered === PIN) {
      unlock();
      overlay.classList.add('az-pin--out');
      setTimeout(function () {
        root.classList.remove('az-locked');
        if (overlay.parentNode) overlay.parentNode.removeChild(overlay);
        onReady(mountLockButton);
      }, 260);
      return;
    }
    attempts++;
    entered = '';
    renderDots();
    shake();
    if (attempts >= MAX) startLockout();
    else setMsg('รหัสไม่ถูกต้อง ลองใหม่');
  }
  function startLockout() {
    lockedUntil = Date.now() + LOCKOUT;
    padEl.classList.add('is-disabled');
    (function tick() {
      var remain = Math.ceil((lockedUntil - Date.now()) / 1000);
      if (remain > 0) { setMsg('ใส่ผิดหลายครั้ง กรุณารอ ' + remain + ' วินาที'); setTimeout(tick, 500); }
      else { attempts = 0; padEl.classList.remove('is-disabled'); setMsg(''); }
    })();
  }

  overlay.addEventListener('click', function (e) {
    var t = e.target;
    if (!t || !t.getAttribute) return;
    if (t.hasAttribute('data-d')) press(t.getAttribute('data-d'));
    else if (t.hasAttribute('data-back')) back();
  });
  document.addEventListener('keydown', function (e) {
    if (!root.classList.contains('az-locked')) return;
    if (e.key && e.key.length === 1 && e.key >= '0' && e.key <= '9') { press(e.key); e.preventDefault(); }
    else if (e.key === 'Backspace') { back(); e.preventDefault(); }
  });

  try { overlay.tabIndex = -1; overlay.focus(); } catch (e) {}
})();
```

- [ ] **Step 2: ตรวจ syntax**

Run: `node --check assets/pin-gate.js`
Expected: ไม่มี output (exit 0)

- [ ] **Step 3: Commit**

```bash
git add assets/pin-gate.js
git commit -m "feat(pin-gate): add gate logic, keypad, lockout, lock button"
```

---

### Task 4: ต่อ include เข้า 11 หน้า entry

**Files:**
- Create (ชั่วคราว): `wire-pin-gate.py` (ลบทิ้งหลังใช้)
- Modify: `index.html` + เครื่องย่อย 10 ตัว

- [ ] **Step 1: ตรวจว่า working tree สะอาดพอ (ดู Preconditions)**

Run: `git status --short index.html */index.html`
Expected: ควรว่าง (ถ้ายังมี `M` ให้ commit/stash ก่อนตาม Preconditions ด้านบน)

- [ ] **Step 2: เขียนสคริปต์แทรก include (idempotent)**

สร้าง `wire-pin-gate.py` ที่ราก repo:

```python
#!/usr/bin/env python3
"""แทรก include ของ pin-gate เข้าไปหลัง <head> ของทุกหน้า entry (idempotent)."""
FILES = {
    'index.html': 'assets/',
    'lifeready/index.html': '../assets/',
    'ihealthy/index.html': '../assets/',
    'ci123/index.html': '../assets/',
    'ishield/index.html': '../assets/',
    'fhc/index.html': '../assets/',
    'career-agent/index.html': '../assets/',
    'agency/index.html': '../assets/',
    'global-saving/index.html': '../assets/',
    'pension-smart95/index.html': '../assets/',
    'group-insurance/index.html': '../assets/',
}

def block(p):
    return (
        '\n  <link rel="stylesheet" href="%spin-gate.css" />'
        '\n  <script src="%spin-gate.config.js"></script>'
        '\n  <script src="%spin-gate.js"></script>'
    ) % (p, p, p)

for path, prefix in FILES.items():
    with open(path, encoding='utf-8') as f:
        html = f.read()
    if 'pin-gate.js' in html:
        print('skip (already wired):', path); continue
    low = html.lower()
    h = low.find('<head')
    if h == -1:
        print('WARN: no <head> in', path); continue
    gt = html.find('>', h)                 # รองรับทั้ง <head> และ <head ...>
    insert_at = gt + 1
    new = html[:insert_at] + block(prefix) + html[insert_at:]
    with open(path, 'w', encoding='utf-8') as f:
        f.write(new)
    print('wired:', path)
```

- [ ] **Step 3: รันสคริปต์**

Run: `python3 wire-pin-gate.py`
Expected: พิมพ์ `wired: <ไฟล์>` ครบ 11 บรรทัด (ถ้าเคยรันแล้วจะเป็น `skip`)

- [ ] **Step 4: ตรวจว่าเข้าครบทุกหน้า**

Run: `grep -l "pin-gate.js" index.html */index.html | wc -l`
Expected: `11`

Run: `grep -c "pin-gate.js" index.html */index.html | grep -v ":1$" || echo OK`
Expected: `OK` (แต่ละไฟล์มี include เดียว ไม่ซ้ำ)

- [ ] **Step 5: ตรวจ path prefix ของ Hub vs เครื่องย่อย**

Run: `grep "pin-gate.js" index.html`
Expected: `<script src="assets/pin-gate.js"></script>` (ไม่มี `../`)

Run: `grep "pin-gate.js" lifeready/index.html`
Expected: `<script src="../assets/pin-gate.js"></script>` (มี `../`)

- [ ] **Step 6: ลบสคริปต์ชั่วคราว**

Run: `rm wire-pin-gate.py`

- [ ] **Step 7: Commit**

```bash
git add index.html lifeready/index.html ihealthy/index.html ci123/index.html ishield/index.html fhc/index.html career-agent/index.html agency/index.html global-saving/index.html pension-smart95/index.html group-insurance/index.html
git commit -m "feat(pin-gate): wire gate into Hub + all 10 tool pages"
```

---

### Task 5: ทดสอบพฤติกรรมในเบราว์เซอร์ (manual)

โปรเจกต์นี้ไม่มี test runner (vanilla) — ทดสอบด้วยเบราว์เซอร์จริงตาม checklist

**Files:** ไม่แก้ไฟล์ (verification เท่านั้น)

- [ ] **Step 1: เปิด local server**

Run: `python3 -m http.server 8000`
เปิด `http://localhost:8000`

- [ ] **Step 2: เดินตาม checklist** (ทำเครื่องหมายเมื่อผ่าน)

- [ ] เปิด Hub ครั้งแรก → เห็น keypad บังหน้าทันที ไม่เห็นเนื้อหาจริงแว้บก่อน
- [ ] ใส่ PIN `123456` → ฉากล็อก fade ออก หน้าแสดง + เห็นปุ่มล็อกลอยมุมล่างซ้าย
- [ ] รีเฟรชหน้าเดิม (F5) → ยังปลดอยู่ ไม่ถามซ้ำ
- [ ] คลิกการ์ดเข้าเครื่องมือ (เช่น LifeReady) → ผ่านเลย ไม่ถามซ้ำ
- [ ] เปิด `http://localhost:8000/ihealthy/` ตรงในแท็บใหม่ (เบราว์เซอร์เดิม) → ผ่านเลย (localStorage แชร์)
- [ ] เปิดหน้าต่าง **incognito** → `http://localhost:8000/ihealthy/` → **ติดรหัสก่อน** (deep link ถูกกัน)
- [ ] ในหน้า keypad ใส่รหัสผิด → 6 จุดสั่น (shake) + ล้าง + ขึ้นข้อความ "รหัสไม่ถูกต้อง"
- [ ] ใส่ผิดครบ 5 ครั้ง → keypad จางลง กดไม่ได้ + นับถอยหลัง "กรุณารอ N วินาที" จนครบ 30 วิ แล้วกลับมากดได้
- [ ] พิมพ์จากคีย์บอร์ด (เลข + Backspace) บนคอม → ใช้ได้เหมือนแตะปุ่ม
- [ ] กดปุ่มล็อกลอย → หน้ารีโหลด + ถาม PIN ใหม่ทันที
- [ ] (จำลองหมดเวลา) DevTools Console: `JSON.parse(localStorage.az_gate)` ดู `ts` → รัน`var s=JSON.parse(localStorage.az_gate); s.ts-=13*3600*1000; localStorage.az_gate=JSON.stringify(s);` แล้วรีเฟรช → ถาม PIN ใหม่ (เกิน idle 12 ชม.)
- [ ] มือถือ (หรือ DevTools responsive): ปุ่ม keypad ขนาดพอเหมาะ แตะง่าย
- [ ] แก้ `pin` ใน `assets/pin-gate.config.js` เป็นเลขอื่น → รหัสใหม่ใช้ได้ รหัสเก่าใช้ไม่ได้ (คืนค่าเดิมหลังทดสอบ)

- [ ] **Step 3: ปิด server** (Ctrl+C) — งานเสร็จ ไม่มีไฟล์ต้อง commit เพิ่ม

---

## Self-Review (ผู้เขียนแผนตรวจแล้ว)

- **Spec coverage:** ครบทุกข้อ — soft PIN gate (T1,T3), ทุกหน้า (T4), localStorage + idle 12h sliding (T3 `isUnlocked`/`unlock`), plaintext config (T1), fail-closed config error (T3), in-memory fallback (T3), keypad + คีย์บอร์ด + lockout + shake (T3), ปุ่มล็อก (T3), กัน FOUC (T2 `html.az-locked` + T3 sync ใน head), deep-link ติดรหัส (T4 + T5 checklist), manual test (T5)
- **Placeholder scan:** ไม่มี TBD/TODO — โค้ดครบทุกไฟล์
- **Type/naming consistency:** ตรวจชื่อ class/ฟังก์ชันตรงกันทุก task — `az-locked`, `az-pin`, `az-pin__key`, `data-d`, `data-back`, `az-lock-btn`, `PIN_GATE_CONFIG`, `az_gate` ตรงกันระหว่าง CSS (T2), JS (T3), config (T1)
