{
  "name": "advisortool",
  "version": "1.0.0",
  "private": true,
  "description": "Advisortool — static HTML/CSS/JS site with prebuilt calculator bundles",
  "scripts": {
    "dev": "python3 -m http.server 8080",
    "dev:ai": "node server.mjs",
    "start": "node server.mjs",
    "test": "node --test tests/*.test.mjs",
    "test:pin-gate": "node --test tests/pin-gate.test.mjs",
    "test:smoke": "node --test tests/static-smoke.test.mjs"
  }
}
